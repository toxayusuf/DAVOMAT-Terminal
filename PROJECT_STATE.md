# DAVOMAT — PROJECT STATE

Версия: v1.1.1
Дата: 2026-09-16

## Зафиксированная архитектура
- Google Apps Script: backend + admin UI + device/enrollment launch links.
- Google Sheets: сотрудники, графики, посещаемость, зарплата, audit.
- Google Drive: контрольные фото.
- GitHub Pages: Face Terminal/PWA.

## Текущий рабочий путь
- Конфигурация устройства передаётся один раз в URL fragment `#cfg=...` и сохраняется в localStorage браузера.
- GET/reads: JSONP к Apps Script (`bootstrap`, `enrollment`, `pendingEnrollment`, `requestStatus`).
- Writes: `fetch(..., mode=no-cors, Content-Type=text/plain)` → Apps Script `doPost` → polling `requestStatus` через JSONP.
- Ни iframe bridge, ни `window.opener`, ни ручной ввод URL/device/token не используются.

## Исправление v1.1.1 — камера
Реальный тест 16.09 показал на iPhone и desktop: терминал открывается, но `getUserMedia` возвращает `NotAllowedError / Permission denied`. Это браузерное/OS разрешение камеры, которое JavaScript не может принудительно включить.

В v1.1.1:
- запрос камеры идёт первым, непосредственно из клика `КЕЛДИ/КЕТДИ/РЎЙХАТГА ОЛИШ`, что важно для iOS;
- сначала используется мягкий `facingMode: ideal`, затем fallback `video:true`;
- добавлена проверка HTTPS / MediaDevices / camera permission;
- при блокировке больше нет технического toast на английском: показывается понятное окно на узбекском с инструкцией для iPhone/iPad, Android и Chrome/Edge;
- есть кнопка `ҚАЙТА СИНАШ`, которая повторяет тот же режим после выдачи разрешения;
- при enrollment камера больше не запускается автоматически без пользовательского жеста: показывается `РЎЙХАТГА ОЛИШ`, затем камера;
- PWA cache поднят до v1.1.1.

## Важное ограничение платформы
Если пользователь уже нажал `Don't Allow / Block`, сайт не может сам снять этот запрет. Разрешение камеры меняется только в браузере/ОС пользователем. После одного разрешения на складском планшете это действие больше не требуется.

## UX правила
- Никакого ручного ввода Apps Script URL, device ID, token или 6-значного кода сотрудником.
- Камера не работает постоянно.
- Регистрация: админ создаёт задачу → сотрудник нажимает `РЎЙХАТГА ОЛИШ` → камера → 0/6 → 6/6 → готово.
- Сотрудник: `КЕЛДИ` / `КЕТДИ` → лицо → результат.

## Проверки
- app.js v1.1.1: `node --check` — OK.
- GitHub Pages workflow run 35068019806 — SUCCESS.
- Apps Script для camera-permission fix менять не требуется.

## Следующий тест
1. На терминале должна отображаться версия `v1.1.1`.
2. Нажать `КЕЛДИ`.
3. Если браузер спросит доступ к камере — выбрать Allow / Разрешить.
4. Если доступ ранее был запрещён — DAVOMAT сам покажет точную инструкцию для текущей платформы и кнопку `ҚАЙТА СИНАШ`.
5. После камеры: лицо → запись → проверить ATTENDANCE_EVENTS / ATTENDANCE.
